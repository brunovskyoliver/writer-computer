# Tasks: LaTeX Suite — Math Snippets and Source Highlighting

**Input**: Design documents from `SPECs/latex-suite/`

**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/ (snippet-file-format, ipc-and-events, editor-extension, settings), quickstart.md

**Tests**: Included. The plan's Constitution "Quality gates" row commits every pure engine module to a unit test and the Rust helpers to `cargo test`; quickstart.md lists the test files by name. The test runner is node-only (no jsdom), so every test targets `EditorState`/plain-string APIs — never `EditorView`.

**Organization**: Tasks are grouped by user story. US1 (snippets) and US3 (highlighting) share only Phase 1–2 work; US2 (edit + hot reload) builds on the store from Phase 2; US4 (settings) adds the last switches on top of US1–US3.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies on unfinished tasks)
- **[Story]**: Which user story this task belongs to (US1–US4)
- Every task names the exact file(s) it touches

## Path Conventions

Repo root is `/Users/oliver/Programming/writer-computer`. All paths below are relative to it.

- Frontend: `apps/desktop/src/`, tests in `apps/desktop/tests/`
- Rust: `apps/desktop/src-tauri/src/`
- Shared assets: `apps/desktop/shared/`
- Docs: `docs/`, `CHANGELOG.md`, `TODOS.md`

Validation commands: `vp check && vp test` (repo root); `cargo test && cargo clippy && cargo fmt --check` (from `apps/desktop/src-tauri/`).

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Dependencies, the single-source tables and assets every later phase imports.

- [x] T001 Add `acorn` to `apps/desktop/package.json` dependencies (use `vp add acorn` from `apps/desktop/`; add a catalog entry in the workspace `pnpm-workspace.yaml` if the repo pins versions there, matching how `@codemirror/language-data` is declared). Confirm `import("acorn")` type-checks under `vp check`.
- [x] T002 [P] Create `apps/desktop/src/lib/latex-snippets/options.ts` exporting the single tables the parser, compiler, UI and tests import: `OPTION_LETTERS = "tmnMArw"`, `MODE_LETTERS = "tmnM"` with a `letterToMode` map (`t→"text"`, `m→"math"`, `n→"inline"`, `M→"display"`), `type SnippetMode = "text" | "math" | "inline" | "display"`, `ENTRY_ERROR_CODES` as a `const` tuple containing exactly `not-an-object, missing-field, invalid-options, invalid-pattern, missing-capture, unknown-variable, invalid-priority, function-replacement, unsupported-value, pattern-timeout, pattern-threw, invalid-variable`, `type EntryErrorCode`, `type EntryError = { code; index; line?; trigger?; message }`, `type LoadError = { message; line?; column? }`, `PATTERN_TIME_BUDGET_MS = 20`, and `SNIPPET_FILE_NAME = "latex-snippets.js"`.
- [x] T003 [P] Create `apps/desktop/shared/latex-snippets.default.js` from `SPECs/obsidian-latex-suite` per research R4 and contracts/snippet-file-format.md "Shipped default set": (a) header comment explaining the format and pointing at `docs/latex-suite.md`; (b) replace the priority-2 "insert space after Greek letters and symbols" function entry with the declarative entry `{trigger: "\\\\(?!(?:${GREEK}|${SYMBOL}|${MORE_SYMBOLS})$)(${GREEK}|${SYMBOL}|${MORE_SYMBOLS})([A-Za-z])", replacement: "\\\\[[0]] [[1]]", options: "rmA", priority: 2}` (escape so the parsed trigger source is `\\(?!…)` and the replacement is `\[[0]] [[1]]`); (c) delete the priority-3 "disable snippets while typing macros" function entry (it becomes an engine rule); (d) delete the `iden(\d)` identity-matrix entry and the list-aware `dm` function entry, keeping plain `dm` (`tAw`) and `/(\S\s*)dm/`; (e) change every `${SYMBOLS}` to `${SYMBOL}`; (f) leave the commented-out "convert standalone letters to math" rules commented. Keep every other entry byte-identical. File must be a bare array or `export default [...]` with no other statements.
- [x] T004 [P] Add five entries to `apps/desktop/shared/settings.schema.json` under category `LaTeX Suite`, scope global, exactly as contracts/settings.md specifies: `latex.snippets-enabled` (boolean, default `true`, label "Snippets"), `latex.tab-out` (boolean, `true`, "Tab out of brackets"), `latex.auto-fraction` (boolean, `true`, "Auto fraction"), `latex.highlight-source` (boolean, `true`, "Highlight math source"), `latex.snippet-variables` (list, label "Snippet variables") whose default is the five `NAME=alternation` items `GREEK`, `SYMBOL`, `MORE_SYMBOLS`, `ACCENT`, `SYMBOLS` (copy values from the Obsidian Latex Suite plugin's `default_snippet_variables`; `SYMBOLS` is an alias of `SYMBOL`). Copy the description strings from the contract table. Extend `apps/desktop/tests/settings-schema.test.ts` with an assertion that all five keys exist, carry the listed defaults, and have category `LaTeX Suite`. Run `vp test tests/settings-schema.test.ts` and `cargo test` (the Rust side `include_str!`s the schema and must still parse it).

**Checkpoint**: `vp check` passes; the default snippet file and the schema exist; no runtime behaviour has changed.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: The file → parse → compile → store pipeline and the math-context probe. US1 needs the compiled set, US2 needs the store and Rust path command, US4 needs the variables path.

**⚠️ CRITICAL**: US1 and US2 cannot start before this phase is complete. US3 depends only on T004 and may start in parallel with this phase.

- [x] T005 Create `apps/desktop/src-tauri/src/commands/latex.rs` with two `#[tauri::command]`s per contracts/ipc-and-events.md: `latex_snippets_path(app: AppHandle) -> Result<String, AppError>` resolves `app.path().app_data_dir()?.join("latex-snippets.js")`, creates the parent dir and writes `include_str!("../../../shared/latex-snippets.default.js")` only when the file is missing (never overwrites), returns the absolute path; `reset_latex_snippets(app) -> Result<String, AppError>` writes the same bytes atomically (temp file + rename, same pattern as session saves in the existing code) and returns the path. Factor the pure logic into `fn ensure_snippets_file(dir: &Path) -> io::Result<PathBuf>` and `fn reset_snippets_file(dir: &Path) -> io::Result<PathBuf>` so `#[cfg(test)]` tests with `tempfile`/temp dirs cover: creates once with the default bytes; a second `ensure` leaves a modified file untouched; `reset` restores the default bytes. Add `pub mod latex;` to `apps/desktop/src-tauri/src/commands/mod.rs`.
- [x] T006 Register `commands::latex::latex_snippets_path` and `commands::latex::reset_latex_snippets` in the `tauri::generate_handler![…]` list in `apps/desktop/src-tauri/src/lib.rs`. Run `cargo test && cargo clippy && cargo fmt --check` from `apps/desktop/src-tauri/`.
- [x] T007 Add `getLatexSnippetsPath(): Promise<string>` and `resetLatexSnippets(): Promise<string>` wrappers in `apps/desktop/src/lib/tauri.ts` (same `invoke` style as the neighbouring wrappers) and cover them in `apps/desktop/tests/tauri-ipc.test.ts` the way the existing wrappers are covered.
- [x] T008 [P] Create `apps/desktop/src/lib/latex-snippets/parse-snippet-file.ts` exporting `parseSnippetFile(source: string): Promise<{ entries: SnippetEntry[]; errors: EntryError[] } | { failure: LoadError }>` using a dynamic `await import("acorn")` (`sourceType: "module"`, `ecmaVersion: 2022`, `locations: true`). Accept `export default [...]` or a bare `[...]` expression statement; anything else → `failure`. Per array element produce a `SnippetEntry` (`index`, `line = loc.start.line`, `trigger` as `{kind:"string", value}` for string literals / expression-free template literals or `{kind:"regex", source, flags}` for regex literals, `replacement` string, `options` string, optional `priority` number (allow `UnaryExpression` `-` on a numeric literal), optional `description`), or an `EntryError` with codes from `options.ts`: `not-an-object` (element not an `ObjectExpression`), `missing-field` (any of trigger/replacement/options absent), `function-replacement` (`FunctionExpression`/`ArrowFunctionExpression` replacement), `unsupported-value` (any other unsupported node in a known key, including template literals with `${}` expressions). Ignore unknown keys. Set `trigger` on the error when the trigger is a string literal. A `SyntaxError` thrown by acorn → `failure: { message, line: err.loc.line, column: err.loc.column }`. Never evaluate anything.
- [x] T009 [P] Create `apps/desktop/src/lib/latex-snippets/replacement.ts` exporting `type ReplacementSegment` (the four variants from data-model.md: `literal`, `tabstop {index, placeholder?}`, `capture {group}`, `visual`) and `parseReplacement(text: string): ReplacementSegment[]` recognising `$N` (single digit 0–9), `${N:placeholder}` (placeholder may not contain `}`), `[[n]]` (non-negative integer), `${VISUAL}`; every other `$` and `[[` is literal (e.g. `"$$0$"` → literal `$`, tabstop 0, literal `$`). Also export `maxCaptureIndex(segments): number | null` and `hasVisual(segments): boolean`. `${NAME}` variables are NOT handled here — they are substituted textually before this runs.
- [x] T010 Create `apps/desktop/src/lib/latex-snippets/compile.ts` exporting `parseVariables(items: string[]): { variables: Map<string,string>; errors: EntryError[] }` (split on the first `=`; an item without `=` or with an empty name → `invalid-variable` with `index` = item position) and `compileSnippetSet(entries: SnippetEntry[], variables: Map<string,string>): { set: CompiledSnippetSet; errors: EntryError[] }`. Per entry, in this order and each failure → skip with an `EntryError` carrying the entry's `index`, `line`, `trigger` (when string): substitute every `${NAME}` in trigger source and replacement (`${VISUAL}` and `${digit:…}` are not variables; an unresolved name → `unknown-variable`); validate `options` (letters only from `OPTION_LETTERS`, exactly one of `t m n M`, `r` may be redundant on a regex literal) → `invalid-options`; `priority` present but not finite → `invalid-priority`; build `pattern = new RegExp("(?:" + src + ")$", "u")` where string triggers without `r` are regex-escaped first, `w` prepends `(?<![\p{L}\p{N}_])`, and a regex literal carrying the `v` flag or a `SyntaxError` → `invalid-pattern` with the engine message; compute `captureCount` via `new RegExp(src + "|").exec("").length - 1`; `parseReplacement`; every `[[n]]` must satisfy `n < captureCount` → `missing-capture`. Produce `CompiledSnippet` (`id`, `pattern`, `triggerText` = literal trigger or `null`, `template`, `mode`, `automatic`, `visual`, `priority` default 0, `captureCount`, `description`, `disabled: null`) and the `CompiledSnippetSet` shape from data-model.md: `byMode.text|inline|display.{auto,tab}` (`math` entries go into both `inline` and `display`), each list sorted by priority desc then index asc; `visual: Map<string, CompiledSnippet[]>` keyed by `triggerText` (only visual snippets, per mode as `\`${mode}:${triggerText}\``); `count`. Export `EMPTY_SNIPPET_SET`.
- [x] T011 [P] Create `apps/desktop/src/lib/latex-snippets/math-context.ts` exporting `type MathContext` (from data-model.md: `prose | code | inline {node} | display {node} | text {node}` with `node = { from, to, formulaFrom, formulaTo }`) and `mathContext(state: EditorState, pos: number): MathContext`. Rules from contracts/editor-extension.md "Math context rules": `syntaxTree(state).resolveInner(pos, -1)` walked upward; `InlineCode`/`FencedCode`/`CodeBlock`/`HTMLBlock` → `code`; a `Math` node → `display` if the node's source starts with `$$`, else `inline`, with `formulaFrom/To` from its `MathFormula` child (or the span between delimiters when the formula is empty); inside math, an unclosed `\text{` before `pos` in the formula text (count `{`/`}` after the last `\text{`) → `text`; else `prose`. A caret exactly on the left delimiter counts as outside, on the right delimiter counts as inside. Do not call `ensureSyntaxTree` here — callers own that.
- [x] T012 Create `apps/desktop/src/stores/latex-snippet-store.ts` (Zustand, same style as `settings-store.ts`) with state `filePath: string | null`, `status: SnippetLoadStatus` (`empty | loaded {set, warnings} | failed {error, set}` per data-model.md) and actions per contracts/ipc-and-events.md "Frontend store API": `load()` — the single write path: ensure `filePath` via `getLatexSnippetsPath()`, `readFile(filePath)`, `parseSnippetFile`, `parseVariables(useSettingsStore.getState().settings["latex.snippet-variables"])`, `compileSnippetSet`; guard with a monotonically increasing load sequence number so a stale read never overwrites a newer result; on read/parse failure set `failed` keeping the previous `set` (or `EMPTY_SNIPPET_SET` from `empty`); on success set `loaded` with `warnings = parse errors ++ variable errors ++ compile errors`. `reset()` = `await resetLatexSnippets(); await load()`. `openInEditor()` = `useEditorStore.getState().openFileInNewTab(filePath)` (ensures path first). `getActiveSet()` returns `status.set` for `loaded`/`failed` and `EMPTY_SNIPPET_SET` for `empty`. `markDisabled(id, error)` flips one snippet's `disabled` and appends the error to `warnings` (used by the matcher's time-budget guard). Export a `startLatexSnippetSubscriptions()` that (a) `listen<FileChangeEvent>("fs:file-changed")` and calls `load()` when `event.payload.path === filePath`, and (b) subscribes to `useSettingsStore` for `latex.snippet-variables` changes and calls `load()`; both return their unsubscribe. No other module may read the snippet file or call the two Rust commands.
- [x] T013 Call `useLatexSnippetStore.getState().load()` (non-blocking, `void`) and `startLatexSnippetSubscriptions()` from app startup after settings hydrate, in the same place the other post-hydration store work starts (find it via `hydrateFromBackend` / `loadSettings` callers in `apps/desktop/src/hooks/` or `apps/desktop/src/App.tsx`); tear the subscriptions down in the matching cleanup. Add the store to `apps/desktop/tests/stores.test.ts` only if that file mocks stores generically; otherwise skip.
- [x] T014 [P] Create `apps/desktop/tests/latex-snippet-file.test.ts`: (1) `SPECs/obsidian-latex-suite` (read from the repo path via `node:fs` relative to the test file) parses with exactly four `function-replacement` errors and no other errors, and every other entry becomes a `SnippetEntry`; (2) `apps/desktop/shared/latex-snippets.default.js` parses with zero errors; (3) a bare array and an `export default` array both load; (4) a file with `{trigger: "x"` unclosed yields `failure` with the right `line`; (5) a template literal with `${}` in `replacement` → `unsupported-value`; (6) a non-object element → `not-an-object`; (7) missing `options` → `missing-field` with the string trigger attached; (8) `priority: -1` parses as `-1`.
- [x] T015 [P] Create `apps/desktop/tests/latex-snippet-compile.test.ts` covering `compileSnippetSet` + `parseVariables`: the option-letter validation table (unknown letter, zero mode letters, two mode letters, redundant `r` accepted); `${GREEK}` substitution into a trigger from a variables map; `unknown-variable`; `missing-capture` for `[[1]]` with one group; `invalid-pattern` for an unbalanced `(` and for a `v`-flag literal; `invalid-priority`; `w` produces a pattern that rejects `admin` but accepts ` dm`; sort order = priority desc then index; `math` snippets appear in both `inline` and `display` buckets; visual snippets keyed in `visual`; `parseVariables` reports `invalid-variable` for `GREEK` (no `=`) and `=x` (empty name) and keeps a second `=` in the value.
- [x] T016 [P] Create `apps/desktop/tests/latex-math-context.test.ts` building `EditorState` with the same Markdown language config `createEditorExtensions` uses (import `markdown` with `GFM` + `prosemarkMarkdownSyntaxExtensions`, as `tests/math.test.ts` already does) and asserting `mathContext` for: prose; `$x|$` inline; `$$x|$$` display (single line) and multi-line display; `$\text{a|}$` → `text`; `$\text{a}|$` → `inline`; `` `$x|$` `` inline code → `code`; inside a fenced block → `code`; caret on the left `$` → `prose`; caret on the right `$` → `inline`; `$5| and $6` currency guard → `prose`.

**Checkpoint**: `vp test` passes with the three new test files; the store loads the default set at startup (verify in the running app via the console: `useLatexSnippetStore.getState().status.kind === "loaded"` and `count ≈ 180`). No editor behaviour has changed yet.

---

## Phase 3: User Story 1 — Type math faster with built-in snippets (Priority: P1) 🎯 MVP

**Goal**: Auto and Tab-triggered snippets with nested tabstops, tab-out, auto-fraction, one-undo-step expansion, Backspace-undo, vim/IME/multi-caret guards — driven by the default set the store already holds.

**Independent Test**: Quickstart §1 steps 1–10 in a fresh note. Automated: `latex-snippet-match`, `latex-snippet-expand`, `latex-auto-fraction`, `latex-tab-out`, `latex-default-set` tests.

### Pure engine (no CodeMirror view)

- [x] T017 [P] [US1] Create `apps/desktop/src/lib/latex-snippets/match.ts` exporting `matchSnippet(args: { lineBefore: string; context: MathContext; set: CompiledSnippetSet; kind: "auto" | "tab"; selectionText: string; typedText?: string; onDisable: (id: number, error: EntryError) => void }): SnippetMatch | null` per research R9. Pick the bucket from `context.kind` (`prose→text`, `inline`, `display`; `code`/`text` → return `null`) and `kind`. If `selectionText` is non-empty: only visual snippets, matched by `set.visual.get(mode:typedText)`, return `{ snippet, from: -1, to: -1, groups: [], visualText }` (the caller replaces the selection). Otherwise scan the pre-sorted list, skip `disabled` and `visual` entries, run `pattern.exec(lineBefore)` inside a wrapper that measures `performance.now()` and catches throws — a run > `PATTERN_TIME_BUDGET_MS` or a throw calls `onDisable(id, { code: "pattern-timeout" | "pattern-threw", … })` and continues. First hit wins. Apply the macro guard for `kind === "auto"`: if the match start lies strictly inside a `\[A-Za-z]+` word that ends at the caret (search `/\\[A-Za-z]+$/` on `lineBefore`), skip that snippet. Return `{ snippet, from: lineBefore.length - m[0].length, to: lineBefore.length, groups: m.slice(1) }` (offsets relative to the line; the caller adds `line.from`).
- [x] T018 [P] [US1] Create `apps/desktop/src/lib/latex-snippets/expand.ts` exporting `type TabstopFrame = { marks: DecorationSet; currentGroup: number }` and `expandSnippet(match: SnippetMatch, state: EditorState, replaceFrom: number, replaceTo: number): { changes: ChangeSpec; selection: EditorSelection; frame: TabstopFrame | null }`. Walk `snippet.template`: `literal` → text; `capture {group}` → `match.groups[group] ?? ""`; `visual` → `match.visualText`; `tabstop` → the placeholder text (or empty) and record `{ from, to, group: index }` absolute positions. Store stops in the frame as a `RangeSet` of a small `RangeValue` subclass `TabstopRange { group: number }` (a `Decoration.mark` cannot be empty, and stops usually are), so `frame.marks.map(changes)` tracks edits for free; the view layer (T027) turns only the non-empty ranges into `Decoration.mark({ class: "cm-latex-tabstop" })`. Order groups ascending by index with `$0` first (index 0 < 1 < … < 9, so plain ascending). `selection` = all ranges of the first group (multi-range when repeated); no tabstops → `frame: null` and caret after the inserted text. Also export `selectionForGroup(frame, group, docLength): EditorSelection | null` and `nextGroup(frame, dir: 1 | -1): number | null`.
- [x] T019 [P] [US1] Create `apps/desktop/src/lib/latex-snippets/auto-fraction.ts` exporting `autoFraction(lineBefore: string): { operandFrom: number; text: string; stops: { group: number; offset: number }[] } | null` (offsets relative to the line) per contracts/editor-extension.md "Auto-fraction": `lineBefore` ends with the just-typed `/`; look at the character before it: if it is `)`/`]`/`}` take the balanced group scanning backwards and prepend an immediately preceding control sequence (`\sqrt{x}` → whole); otherwise take the maximal run of characters that are not whitespace and not one of `+ - = < > , ( [ {`; empty operand → `null` (caller keeps the literal `/`). Result text = `\frac{operand}{}` with stop 0 inside the denominator and stop 1 after the closing brace; `operandFrom` is the line offset where the replacement starts (the operand start), and the replaced range ends at the `/`.
- [x] T020 [P] [US1] Create `apps/desktop/src/lib/latex-snippets/tab-out.ts` exporting `tabOutTarget(lineAfter: string, caret: number, context: MathContext, lineTo: number): number | null` per contracts/editor-extension.md "Tab-out": first index of `}`, `)`, `]`, `|` in `lineAfter` → `caret + i + 1`; otherwise if the enclosing `Math` node's `to` ≤ `lineTo` (closes on this line) → `context.node.to`; otherwise `null`.
- [x] T021 [P] [US1] Create `apps/desktop/tests/latex-snippet-match.test.ts` using `compileSnippetSet` over small hand-written entries plus the real default file where noted: `dm` (`tAw`) matches `dm` and ` dm` but not `admin`; `dot` vs `ddot` (priority) → `ddot` wins on `ddot`; `sin` with a `prose` context returns `null`; macro guard: `\tex` + `t` in math does not fire the `text` snippet, but `\alpha` + `x` fires the space rule and `\in` + `t` does not (negative lookahead); visual `S` with `selectionText: "a+b"` and `typedText: "S"` returns the visual snippet and with empty selection returns `null`; non-visual snippets are skipped while a selection exists; a pattern that throws (mock `exec`) calls `onDisable` with `pattern-threw` and the scan continues to the next candidate; `[[0]]` groups are returned.
- [x] T022 [P] [US1] Create `apps/desktop/tests/latex-snippet-expand.test.ts`: `//` (`\frac{$0}{$1}$2`) → text `\frac{}{}`, three groups, first selection is empty at the numerator; `${0:f}` placeholder is inserted and selected; repeated `$0` → two ranges in the selection; `[[0]]` capture and `${VISUAL}` are substituted; a template without tabstops yields `frame: null` and caret at end; `nextGroup` returns `null` after the last group; mapping the frame's `RangeSet` through a `ChangeSet` that inserts text before it shifts every range (proves stops survive edits).
- [x] T023 [P] [US1] Create `apps/desktop/tests/latex-auto-fraction.test.ts`: `(a+b)/` → `\frac{a+b}{}`; `x^2/` → `\frac{x^2}{}`; `\sqrt{x}/` → `\frac{\sqrt{x}}{}`; `a+b/` → `\frac{b}{}`; ` /` and `/` at line start → `null`; `[a,b]/` takes the bracket group; stops at offsets inside `{}` and after the closing brace.
- [x] T024 [P] [US1] Create `apps/desktop/tests/latex-tab-out.test.ts`: `\sqrt{x|}` → past `}`; `x|)` → past `)`; `|x|` at the first pipe → past the second; caret at end of `$…$` content on one line → the node's `to`; multi-line display math with no closer on the line → `null`; nothing ahead in inline math whose `$` is on a later line → `null`.
- [x] T025 [US1] Create `apps/desktop/tests/latex-default-set.test.ts` (SC-001 + SC-004 tripwire): parse `apps/desktop/shared/latex-snippets.default.js` and compile it with the schema's default `latex.snippet-variables` (read from `settings.schema.json` through `src/lib/settings-schema.ts`); assert zero errors and `count` ≥ 150. For every snippet whose `triggerText` is non-null and `captureCount === 0` and not `visual`: build `lineBefore = triggerText` (prefix with a space when `w`), a context matching its mode, call `matchSnippet` then `expandSnippet` on an `EditorState` containing just the trigger, and assert the resulting doc equals the template with `$N`/`${N:…}` reduced to their placeholders and the caret at the first stop. Micro-benchmark: the full `inline.auto` bucket scanned against a 200-char math line with no match completes in < 1 ms averaged over 1,000 runs (assert on the average; skip under `process.env.CI === undefined` only if flaky — prefer keeping it).

### CodeMirror glue

- [x] T026 [US1] Create `apps/desktop/src/components/editor-area/latex-snippets.css` with `.cm-latex-tabstop { background: color-mix(in srgb, var(--accent) 18%, transparent); border-radius: 2px; }` plus the bracket-match rules US3 will reuse: `.cm-matchingBracket { outline: 1px solid color-mix(in srgb, var(--accent) 60%, transparent); }` and `.cm-nonmatchingBracket { color: var(--pm-syntax-invalid); }` — scoped so they only apply inside `.cm-latex-source` (add that class from the highlight/decoration layer in T033/T040). Import it where `math-widget.css` is imported.
- [x] T027 [US1] Create `apps/desktop/src/components/editor-area/latex-snippets-extension.ts` — part 1, the state: `tabstopField: StateField<{ frames: TabstopFrame[]; lastExpansionAt: number | null }>` with effects `pushFrame`, `advanceFrame(dir)`, `clearFrames`; `update` maps every frame's range set through `tr.changes` and drops frames whose set becomes empty; clears all frames on `tr.isUserEvent("writer")`; records `lastExpansionAt = tr.state.doc.length`-independent counter (keep a `docChangeCounter` in the same field: increment on every `tr.docChanged`; set `lastExpansionAt` to the new counter value when `tr.isUserEvent("input.snippet")`). Provide the decorations via `EditorView.decorations.from(tabstopField, s => marks of the top frame's non-empty ranges as Decoration.mark({ class: "cm-latex-tabstop" }))`. Export `latexTabstopState` (field + decorations) and the effects.
- [x] T028 [US1] Same file, part 2, the `inputHandler` at `Prec.high` per contracts/editor-extension.md "Per-keystroke decision": return `false` when `view.composing`, `state.selection.ranges.length > 1`, vim is on and `useVimStore` reports the tab (`getTabId()`) not in `insert`/`replace` mode, or both `latex.snippets-enabled` and `latex.auto-fraction` are off (read `useSettingsStore.getState().settings` each call — no reconfigure). Visual path first: if the selection is non-empty and `set.visual` has an entry for `\`${mode}:${text}\``, do NOT insert — dispatch one expansion transaction replacing `[sel.from, sel.to]` with the visual template (`visualText = selectionText`) and return `true`; if the selection is non-empty and no visual snippet matches, return `false`(ordinary typing replaces the selection). Otherwise capture the typed`text`, `view.dispatch(state.replaceSelection(text))`(the default insert with`userEvent: "input.type"`), then `ensureSyntaxTree(view.state, lineEnd, 20)`, `ctx = mathContext(...)`; `code`/`text`→`true`. If snippets enabled: `matchSnippet({ lineBefore, context: ctx, set: store.getActiveSet(), kind: "auto", selectionText: "", onDisable: store.markDisabled })`; on a hit `replaceFrom = line.from + m.from`, `replaceTo = line.from + m.to`, then `expandSnippet`and`view.dispatch({ changes, selection, effects: [pushFrame(frame)], userEvent: "input.snippet", annotations: isolateHistory.of("full") })`; return `true`. Else if auto-fraction enabled, `text === "/"`, ctx is `inline`/`display`: `autoFraction(lineBefore)`→ dispatch the replacement with a pushed frame built from its stops, same`userEvent`/annotation; return `true`. Return `true` (we dispatched).
- [x] T029 [US1] Same file, part 3, the `Prec.highest` keymap per the contract table. `Tab`: (1) top frame → `nextGroup(frame, 1)`; if `null` pop the frame and repeat with the frame below; when a group is found dispatch `{ selection: selectionForGroup(...), effects: [advanceFrame(1)], userEvent: "select" }` and return `true`; (2) snippets enabled → `matchSnippet({ kind: "tab" })` → expand as in T028; (3) `latex.tab-out` and ctx is math → `tabOutTarget` → dispatch selection-only, `true`; (4) `false`. `Shift-Tab`: previous group in the top frame or `false`. `Escape`: frames present → dispatch `clearFrames`, return `false`. `Backspace`: `lastExpansionAt === docChangeCounter` and `undoDepth(state) > 0` → `undo(view)`, `true`; else `false`. All four return `false` when vim is on and the tab is not in insert/replace mode.
- [x] T030 [US1] Same file, part 4: a `ViewPlugin` that subscribes to `useVimStore` for `getTabId()` and dispatches `clearFrames` when the mode leaves `insert`/`replace` (unsubscribe in `destroy`). Export `latexSnippetsExtension(getTabId: () => string): Extension` = `[EditorState.allowMultipleSelections.of(true), latexTabstopState, Prec.high(EditorView.inputHandler.of(...)), Prec.highest(keymap.of([...])), vimWatcherPlugin]`.
- [x] T031 [US1] Wire it in `apps/desktop/src/components/editor-area/editor-extensions.ts`: append `latexSnippetsExtension(getTabId)` after `prosemarkBaseThemeSetup()` (the exact slot from contracts/editor-extension.md "Extension placement" item 5) and import the CSS. Run `vp check && vp test`, then the app (`vp run dev` from `apps/desktop/`) and walk quickstart §1 steps 1–10. Record any deviation as a follow-up task rather than silently changing behaviour. **Done in code; `vp check` and `vp test` are green (945 tests). The manual quickstart §1 walk in the running app has NOT been performed — it is tracked in TODOS.md.**

**Checkpoint**: US1 is fully usable with the shipped defaults. `mk`, `//`, Tab/Shift-Tab, nesting, undo, Backspace-undo, visual `S`, auto-fraction, tab-out, vim guard and IME guard all behave per quickstart §1.

---

## Phase 4: User Story 2 — Edit the snippet file in-app and see changes live (Priority: P1)

**Goal**: "Edit snippets" opens the file as a JavaScript code tab; saves (in-app or external) hot-reload the set within one second with no UI change; errors show in Settings; "Reset to defaults" restores the shipped file after confirmation.

**Independent Test**: Quickstart §2 steps 1–8.

### Code-flavor editor tab

- [x] T032 [P] [US2] Create `apps/desktop/src/lib/editor-flavor.ts` exporting `type EditorFlavor = "markdown" | { kind: "code"; language: LanguageDescription }` and `editorFlavorForPath(path: string): EditorFlavor` per research R7: `LanguageDescription.matchFilename(languages, path)` (from `@codemirror/language-data`); a match whose `name !== "Markdown"` → code flavor; anything else (including `.txt` and unknown extensions) → `"markdown"`. Add `apps/desktop/tests/editor-flavor.test.ts`: `.md`/`.markdown`/`.txt`/no-extension → markdown; `latex-snippets.js` → code with `language.name === "JavaScript"`; `notes.json` → code.
- [x] T033 [US2] In `apps/desktop/src/components/editor-area/editor-extensions.ts` add a `flavor: EditorFlavor` parameter to `createEditorExtensions` and branch: code flavor returns `[vimModeExtension(getTabId), historyCompartment.of(history()), <the CodeMirror half of prosemarkBasicSetup — dropCursor, closeBrackets, standard/history/search keymaps, EditorView.lineWrapping — check what prosemarkBasicSetup composes and reuse its exported pieces rather than copying>, drawSelection(), await-free language via language.support ?? language.load() handled by a Compartment that starts empty and is reconfigured once load() resolves, generalSyntaxHighlights + the base theme used today, editorSearchExtensions, storeSyncExtension(...), editorClipboardExtension(...), focusOnRevealExtension(isDisposed)]` and nothing Markdown-specific (no fold/hide/list/prosemark syntax, no decorations, no wiki links, no `markdownFormatting`, no `latexSnippetsExtension`, no math highlighting). Markdown flavor keeps today's list. In `apps/desktop/src/components/editor-area/use-prosemark-editor.ts` compute `editorFlavorForPath(getFilePath())` once per tab and pass it; make sure a tab swap to a different flavor rebuilds the view (check how the existing swap path decides to reuse a view and add the flavor to that decision).
- [x] T034 [US2] Verify in the running app that opening `latex-snippets.js` via `openFileInNewTab` shows JavaScript highlighting, saves with ⌘S through the ordinary save path with the saved/unsaved indicator, and that `.md` tabs are unchanged. Check `apps/desktop/tests/editor-api.test.ts` / `polish.test.ts` for any test asserting on the extension list and update it for the new parameter. **Verified in the running app (release e2e build, raw WebDriver session): `latex-snippets.js` opens with JavaScript highlighting (337 token spans, no Markdown decorations), and an edit in the tab saves through the ordinary save path to disk. No test asserted on the extension list, so none needed updating.**

### Hot reload path

- [x] T035 [US2] In `apps/desktop/src-tauri/src/watcher.rs` add `pub fn start_global_config_watcher(app: AppHandle, dir: PathBuf) -> notify::Result<RecommendedWatcher>` per contracts/ipc-and-events.md: non-recursive `notify` watch on `dir`, the same `DEBOUNCE_MS` collect-then-process loop as `start_watcher`/`start_file_watcher`; for each debounced batch, events whose path file name is `latex-snippets.js` → `app.emit("fs:file-changed", FileChangeEvent { path, kind, workspace: None })` (all windows); events for `config` are handled in T047 (US4) — for now log and ignore them; everything else ignored. Add the handle to `AppState` (`apps/desktop/src-tauri/src/state.rs`, `global_config_watcher: Mutex<Option<RecommendedWatcher>>`). In `apps/desktop/src-tauri/src/lib.rs` `.setup(...)` resolve `app.path().app_data_dir()?`, `create_dir_all` it, start the watcher and store the handle; a failure to start is logged with `log::error!` and does not abort startup. `cargo test && cargo clippy && cargo fmt --check`.
- [x] T036 [US2] Confirm `apps/desktop/src/hooks/use-file-watcher.ts` already treats `workspace: null` on `fs:file-changed` as current (it uses `isWorkspaceEventCurrent(event.payload.workspace, …)`) so an open `latex-snippets.js` tab reloads from disk with the existing diskContent comparison; add a case to `apps/desktop/tests/file-watcher.test.ts` asserting a `workspace: null` event for an open path triggers the reload branch. No behaviour change expected — this task is verification plus the test.
- [x] T037 [US2] Verify end to end in the running app (quickstart §2 steps 2–3, 8): save from the tab → the store reloads (console: `status.kind`, `count`) within a second; edit with an external editor → same; the note being edited shows no scroll/caret/selection change; two windows with the tab open both update once. **Verified: an external edit hot-reloads both the open tab (confirmed after scrolling to the changed line — CodeMirror only renders the viewport) and the store (198 → 199 snippets) within the debounce; a broken file shows `Could not load: Unexpected token (399:0) (line 399)` plus `Using the last valid set (199 snippets)`; a bad entry shows `199 loaded, 1 skipped` with the warning row. The in-app save leg is verified by the on-disk result above plus the write ordering in `lib/save.ts` (write → `markSaved` before the 300 ms debounce, `isSaveInFlight` guard). Two-window reload (step 8) and the native reset dialog (step 7) cannot be driven by the harness and remain unverified.**

### Settings section

- [x] T038 [P] [US2] Create `apps/desktop/src/components/settings-panel/latex-suite-extras.tsx` exporting `LatexSuiteExtras` per contracts/settings.md "Section rendering": reads `useLatexSnippetStore` via selectors (`filePath`, `status`); renders the muted file path; **Edit snippets** button → `openInEditor()`; **Reset to defaults** button → `ask("Replace your snippet file with the shipped defaults? This cannot be undone.", { title: "Reset snippets", kind: "warning" })` from `@tauri-apps/plugin-dialog`, then `reset()` on confirm; status line (`"N snippets loaded"`, `"N loaded, M skipped"`, or red `"Could not load: <message> (line L)"` + `"Using the last valid set (N snippets)"`); a warning list with one row per `EntryError`: `#index`, trigger (if any), `line L`, message. Use the existing settings-panel control styling (see `setting-control.tsx` / `themes-section.tsx`) and no new CSS file unless the panel already has one to extend.
- [x] T039 [US2] In `apps/desktop/src/components/settings-panel/index.tsx` add `const SECTION_EXTRAS: Record<string, ComponentType> = { "LaTeX Suite": LatexSuiteExtras }` and render `SECTION_EXTRAS[category]` under each section's schema-driven controls (one lookup in the section loop, no per-category `if`). Run the app: Settings → LaTeX Suite shows the five schema controls (from T004) plus the extras; walk quickstart §2 steps 1, 4, 5, 6, 7. **Verified: the LaTeX Suite section renders the five schema controls plus the extras (path, both buttons, `198 snippets loaded`). Walking step 1 found a defect — a second **Edit snippets** click appended a duplicate tab; fixed in `latex-snippet-store.openInEditor` (focus an existing tab) and re-verified from a wiped data dir: two clicks, one tab.**

**Checkpoint**: US2 complete — edit, external edit, broken file, skipped entry, missing file recreation, reset, and two-window reload all behave per quickstart §2. US1 keeps working throughout.

---

## Phase 5: User Story 3 — Read raw math source with highlighting (Priority: P2)

**Goal**: Five token classes in unfolded math source and `latex`/`tex` fences, theme-driven colours, bracket matching scoped to math, rendered math untouched.

**Independent Test**: Quickstart §3 steps 1–5. Automated: `latex-highlighting.test.ts`. Depends on T004 only (for the `latex.highlight-source` key); can run in parallel with Phases 2–4.

- [x] T040 [P] [US3] Create `apps/desktop/src/components/editor-area/latex-highlighting.ts` — part 1, the language: `latexTextTag = Tag.define()` and `latexSourceLanguage = StreamLanguage.define({ name: "latex", token(stream) { … } })` per research R5, covering every character with exactly one token: `\` + letters or `\` + one non-letter → `"keyword"`; `{ } [ ] ( )` → `"bracket"`; `^ _ &` and `\\` → `"operator"`; digits (with optional decimal part) → `"number"`; `%` to end of line → `"lineComment"`; `$` / `$$` → `"processingInstruction"`; anything else (letters, punctuation, whitespace — consume runs) → the custom text tag via `tokenTable: { latexText: latexTextTag }` returning `"latexText"`. Export `latexMathNesting: MarkdownConfig = { wrap: parseMixed(n => n.name === "MathFormula" ? { parser: latexSourceLanguage.parser } : null) }` and `latexAwareCodeLanguages(info: string)`: `info.trim().toLowerCase()` in `{latex, tex}` → `latexSourceLanguage`, else `LanguageDescription.matchLanguageName(languages, info, true)`.
- [x] T041 [US3] Same file, part 2, styles: `colouredLatexStyle = HighlightStyle.define([...], { scope: latexSourceLanguage, all: { fontFamily: "var(--pm-code-font, monospace)", fontSize: "var(--pm-editor-font-size)" } })` mapping keyword → `var(--pm-syntax-keyword)`, bracket → `var(--pm-syntax-atom)`, operator → `var(--pm-syntax-special-variable-macro)`, number → `var(--pm-syntax-literal)`, lineComment → `var(--pm-syntax-comment)`, processingInstruction → `var(--pm-muted-color)`, `latexTextTag` → no colour (check the exact variable names in `apps/desktop/src/lib/prosemark-core/syntaxHighlighting.ts` and `prosemark-theme.css`, and reuse the code-font/size variables they already use rather than inventing new ones); `plainLatexStyle` with only `all`. A `Compartment` holding `syntaxHighlighting(colouredLatexStyle)` or `syntaxHighlighting(plainLatexStyle)` depending on `latex.highlight-source`, driven by a `ViewPlugin` that subscribes to `useSettingsStore` and dispatches one `compartment.reconfigure` on change (copy the pattern from `vim-mode.ts`). Export `latexHighlighting(): Extension` = `[compartment.of(initial), plugin, scopedBracketMatching]`.
- [x] T042 [US3] Same file, part 3, bracket matching: `scopedBracketMatching = bracketMatching({ renderMatch: (match, state) => insideLatexSource(state, match.start.from) ? defaultMarks(match) : [] })` where `insideLatexSource` walks `syntaxTree(state).resolveInner(pos, 1)` upward looking for `MathFormula` or a `FencedCode` whose `CodeInfo` text is `latex`/`tex`, and `defaultMarks` reproduces the default renderer: `Decoration.mark({ class: match.matched ? "cm-matchingBracket" : "cm-nonmatchingBracket" })` on `match.start` and, when present, `match.end`. Also add an `EditorView.decorations`-free way to tag latex source for the CSS scope in T026: simplest is a `Decoration.mark({ class: "cm-latex-source" })` line-independent mark over each `MathFormula` / latex fence body produced by a small `ViewPlugin` over the visible ranges — or, if the nested tree already yields a wrapping class via `classHighlighter`, use that and drop the extra plugin. Pick the one with less code and document the choice in a comment. **Chosen: neither.** `latexHighlighting()` installs the app's only `bracketMatching` (grep confirmed `prosemark-core` has `closeBrackets` but no matcher), so `renderMatch` returning `[]` outside LaTeX source is already the whole scope — the `.cm-latex-source` prefix was dropped from `latex-snippets.css` and no tagging plugin exists.
- [x] T043 [US3] Wire it: in `apps/desktop/src/components/editor-area/editor-extensions.ts` change `markdown({ codeLanguages: languages, extensions: [...] })` to `markdown({ codeLanguages: latexAwareCodeLanguages, extensions: [GFM, prosemarkMarkdownSyntaxExtensions, htmlBlockParserExtension, latexMathNesting] })` and add `latexHighlighting()` after `prosemarkBaseThemeSetup()` (before `latexSnippetsExtension`). In `apps/desktop/src/lib/prosemark-core/syntaxHighlighting.ts` keep the `mathFormulaTag` rule as is (it still styles unmounted/empty formulas) and add a comment pointing at `latex-highlighting.ts` for nested tokens. Confirm `apps/desktop/tests/math.test.ts` and `syntax-highlighting.test.ts` still pass (the `MathFormula` node now has children). **Regression found and fixed:** `parseMixed` _replaces_ `MathFormula` with the mounted tree root, so `node.getChild("MathFormula")` returns `null` — `math-decorations.ts` stopped folding math entirely and `math-context.ts` fell through to its delimiter fallback. Both now call a new `mathFormulaSpan(math)` in `prosemark-core/markdown/mathMarkdown.ts` that reads the span between the two `MathMark` children (single source of truth for the node shape). `math.test.ts` and `latex-math-context.test.ts` now parse with `latexMathNesting` so they actually guard this.
- [x] T044 [P] [US3] Create `apps/desktop/tests/latex-highlighting.test.ts`: tokenise `\frac{a}{b} & x^2 % note \\ 3.5` with `latexSourceLanguage.parser` and assert the five classes appear on the expected spans (walk the tree with `highlightTree` or the node names; assert `\frac` keyword, `{`/`}` bracket, `&`/`^`/`\\` operator, `2`/`3.5` number, `% note` comment, whitespace/letters as latexText); build a Markdown `EditorState` with `latexMathNesting` and assert `MathFormula` in `$x^2$` has a mounted child tree with an operator token; assert `latexAwareCodeLanguages("latex")`, `("TeX")` return `latexSourceLanguage` and `("ts")` returns the TypeScript description; assert a ` ```latex ` fence body is parsed by the LaTeX language (find a `keyword`-tagged node inside `FencedCode`).
- [x] T045 [US3] Run the app and walk quickstart §3 steps 1–5 (five distinct colours light and dark, bracket outline and error colour, `latex`/`tex` fences, folded widget unchanged, theme switch). Check the KaTeX widget path in `math-decorations.ts` still folds identically — the `Math` node shape is unchanged; only `MathFormula` gained children. **Walked as `apps/desktop/e2e/specs/latex-highlighting.spec.js`** (6 checks, screenshots under `VERIFY_SHOT_DIR`), all five steps observed in both light and dark; `specs/latex-math.spec.js` re-run green, so the KaTeX fold is unchanged. Three things the walk turned up:
  - **Bug fixed:** the `.cm-nonmatchingBracket` error colour was invisible. The decoration wraps the token span instead of replacing it, and CodeMirror injects the highlight style after the app stylesheet, so the LaTeX bracket colour won on both the mark and its inner span. `latex-snippets.css` now scopes both through `.cm-content`. The matching outline was never affected (it is an `outline`, not a `color`).
  - **Quickstart §3 step 2 is imprecise:** `matchBrackets` scans the whole document, not the fence, and only filters by token type. Deleting a `}` usually re-pairs the `{` with a later LaTeX `}` elsewhere in the file (shown as _matched_); the error colour needs a genuinely mismatched closer, so the fixture uses `\frac{a)`.
  - **Harness:** `tauri-webdriver` + Node 24+ could not create a session at all (`UND_ERR_INVALID_ARG`); wdio's explicit `Content-Length` is rejected by Node's undici. `e2e/wdio.conf.js` now strips that header. A WebDriver click inside `.cm-content` also does not move the CodeMirror caret, so both LaTeX specs navigate by keyboard; `latex-math.spec.js` additionally reloads after seeding so the sidebar actually lists the file.

**Checkpoint**: US3 complete and independent of the snippet engine — works with `latex.snippets-enabled` off.

---

## Phase 6: User Story 4 — Tune behaviour in Settings (Priority: P3)

**Goal**: The four toggles and the variables list take effect immediately in every window; global setting changes propagate to other windows.

**Independent Test**: Quickstart §4 steps 1–5.

- [ ] T046 [US4] Verify the runtime reads exist and add what is missing: `latex.snippets-enabled`, `latex.tab-out`, `latex.auto-fraction` are read per keystroke/Tab in `apps/desktop/src/components/editor-area/latex-snippets-extension.ts` (T028/T029) and `latex.highlight-source` drives the compartment in `latex-highlighting.ts` (T041); `latex.snippet-variables` changes trigger `load()` through the store subscription (T012). Walk quickstart §4 steps 1–4 in the app and fix any toggle that needs a restart or moves the caret.
- [ ] T047 [US4] In `apps/desktop/src-tauri/src/config.rs` add `Settings::reload_global(&mut self) -> std::io::Result<()>` that re-reads only the global `config` file into the `global` layer and `global_raw` (missing file → empty layer, not an error; defaults and workspace layer untouched). `#[cfg(test)]`: write `latex.tab-out = false` to a temp global dir → `reload_global` → `get_global_or_default("latex.tab-out")` is `false`; delete the file → reload → default again. Then complete the `config` branch in `start_global_config_watcher` (`apps/desktop/src-tauri/src/watcher.rs`): for each label in `AppState::labels()` take `global_settings_file_lock`, `reload_global()` on that window's `Settings`, re-apply telemetry from the settings snapshot exactly as `with_global_settings_mut` in `apps/desktop/src-tauri/src/commands/settings.rs` does (extract a shared helper if that logic is not already callable — one place per concern), then `app.emit("settings:changed", Option::<WorkspaceIdentity>::None)` once. `cargo test && cargo clippy && cargo fmt --check`.
- [ ] T048 [US4] In `apps/desktop/src/hooks/use-file-watcher.ts` change the `settings:changed` listener type to `listen<WorkspaceIdentity | null>` (the handler already passes the payload to `isWorkspaceEventCurrent`, which treats `null` as current); update `apps/desktop/tests/file-watcher.test.ts` with a `null`-payload case asserting `loadSettings()` is called. Then verify quickstart §4 step 5 with two windows: change a LaTeX toggle in A → B's Settings shows it and B's editor behaves accordingly, without restart, and the writing window does not double-reload visibly.

**Checkpoint**: All four stories complete.

---

## Phase 7: Polish & Cross-Cutting Concerns

- [ ] T049 [P] Create `docs/latex-suite.md` (owning doc): file location and format (link the contract), option letters, replacement syntax, priority/file-order rule, the macro guard, the four dropped/replaced reference-file entries (research R4 table), undo semantics (expansion = one step; Backspace right after an expansion undoes it; Tab adds no step), multi-caret rule (expansion suppressed with more than one caret), vim/IME rules, hot-reload behaviour and error surfacing, the line-only lookbehind difference from Obsidian, and the `allowMultipleSelections` side effect (Mod-d now creates real multi-cursors). Add it to the Docs Index in `CLAUDE.md` under Guidelines.
- [ ] T050 [P] Add to `docs/keyboard-shortcuts.md` a "LaTeX Suite (inside math)" block: `Tab` (next tabstop → Tab-snippet → tab-out), `Shift-Tab` (previous tabstop), `Escape` (clear tabstops), `Backspace` right after an expansion (undo it).
- [ ] T051 [P] Add a `CHANGELOG.md` entry under the unreleased section: LaTeX snippets with tabstops, tab-out and auto-fraction; global editable snippet file with silent hot reload and error display in Settings; math source and `latex`/`tex` fence highlighting with bracket matching; five new LaTeX Suite settings; global setting changes now propagate to other open windows; `Mod-d` now creates multi-cursors in Markdown tabs.
- [ ] T052 Measure SC-004 per quickstart §5 (5,000-line note, DevTools Performance, p95 keypress → paint with snippets on vs off) and record both numbers in this file under a "Checkpoint results" heading; if the delta exceeds 2 ms, profile the inputHandler (`ensureSyntaxTree` budget, regex count) before changing the approach.
- [ ] T053 Run the full gate: `vp check && vp test` at the repo root and `cargo test && cargo clippy && cargo fmt --check` in `apps/desktop/src-tauri/`; fix anything red.
- [ ] T054 Update `TODOS.md`: move the LaTeX Suite entry to Done with a one-line summary and links to `SPECs/latex-suite/` and `docs/latex-suite.md`; note the R4 decision (four function entries, not two) for the user to confirm.

---

## Dependencies & Execution Order

### Phase dependencies

- **Phase 1 (Setup)**: no dependencies; T002–T004 run in parallel after T001 starts.
- **Phase 2 (Foundational)**: needs T001–T004. Blocks US1 and US2. T005→T006→T007 is a chain; T008, T009, T011 are parallel; T010 needs T008+T009; T012 needs T007+T010+T011; T013 needs T012; T014–T016 parallel once their modules exist.
- **Phase 3 (US1)**: needs Phase 2. T017–T020 parallel (pure modules); T021–T024 parallel with each other once their modules exist; T025 needs T017+T018; T027→T028→T029→T030→T031 sequential (one file); T026 parallel.
- **Phase 4 (US2)**: needs Phase 2 (store, Rust path command). T032, T038 parallel; T033 needs T032; T034 needs T033; T035→T036→T037 chain; T039 needs T038 + T004. Does not need US1 — but manual verification of "type the new trigger" (quickstart §2.2) needs US1 shipped.
- **Phase 5 (US3)**: needs only T004. Fully parallel with Phases 2–4. T040→T041→T042→T043 sequential (one file, then wiring); T044 parallel with T041–T043; T045 last.
- **Phase 6 (US4)**: needs US1 (T046 toggles), US3 (highlight toggle), and T035 (watcher exists) for T047. T047 and T048 can run in parallel.
- **Phase 7 (Polish)**: needs all stories. T049–T051 parallel; T052–T054 sequential at the end.

### User story completion order

1. US1 (P1) — MVP, usable with defaults.
2. US2 (P1) — editing + hot reload; independent of US1's engine glue but only _demonstrable_ with US1.
3. US3 (P2) — independent; can be built by a second person while US1/US2 are in flight.
4. US4 (P3) — last, thin.

### Parallel examples

**Phase 1** (after T001 is in):

```
T002 options.ts  |  T003 latex-snippets.default.js  |  T004 settings.schema.json + test
```

**Phase 2**:

```
T005→T006→T007 (Rust + wrapper)   ||   T008 parse   |  T009 replacement  |  T011 math-context
                                          └──────┬───────┘
                                              T010 compile
                                                  │
                                   T012 store  ← ─┘ (+T007, +T011)
                                                  │
                                              T013 startup
T014 | T015 | T016 tests as soon as their module lands
```

**Phase 3 (US1)**:

```
T017 match | T018 expand | T019 auto-fraction | T020 tab-out       T026 css
T021 | T022 | T023 | T024 tests                                    T027→T028→T029→T030 (extension file)
T025 default-set test (after T017+T018)                                    └→ T031 wire + manual
```

**Cross-phase**: Phase 5 (US3, T040–T045) can run alongside Phases 2–4 entirely — different files, only T043 touches `editor-extensions.ts`, which T031/T033 also edit; land T031 first or rebase.

---

## Implementation Strategy

**MVP = Phase 1 + Phase 2 + Phase 3 (US1)**: default snippets, tabstops, tab-out, auto-fraction, undo semantics. Ship/verify that first — it is the whole reason the feature exists and exercises every risky piece (inputHandler ordering, history isolation, multi-selection, vim guard).

**Increment 2 = Phase 4 (US2)**: code-flavor tab, global watcher, Settings extras. Verified by editing the file live.

**Increment 3 = Phase 5 (US3)**: highlighting — can be built in parallel by a second worktree (`docs/workflows/worktrees.md`); merge touches only `editor-extensions.ts`.

**Increment 4 = Phase 6 + 7**: toggles, multi-window config propagation, docs, changelog, performance numbers.

Per `CLAUDE.md`: one task at a time in loop mode, commit after each, move the TODOS entry between sections, and keep `docs/latex-suite.md` in sync with any rule that changes during implementation.

## Checkpoint results

_(filled in by T052)_

- SC-004 p95 keypress→paint, snippets on: — ms
- SC-004 p95 keypress→paint, snippets off: — ms
